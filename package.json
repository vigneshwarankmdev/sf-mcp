{
  "name": "salesforce-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for creating Salesforce Sent_Message_vod__c records and listing Accounts.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js http",
    "start:stdio": "node src/index.js stdio",
    "check": "node --eval \"import('./src/index.js').then(() => console.log('import-ok'))\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "jsforce": "^3.10.6",
    "zod": "^4.1.5"
  }
}